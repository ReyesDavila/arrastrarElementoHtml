//Arrastrar elemento
		let isTouchDevice ='ontouchstart' in window; let document_mouse_x = 0;let document_mouse_y = 0;function mouseAction(event) {if(!event.type.includes("end")){document_mouse_x = isTouchDevice ? event.touches[0].pageX : event.pageX;document_mouse_y = isTouchDevice ? event.touches[0].pageY : event.pageY;}}document.addEventListener(isTouchDevice ? "touchstart" : "mousedown", mouseAction, true);document.addEventListener(isTouchDevice ? "touchmove" : "mousemove", mouseAction, true);document.addEventListener(isTouchDevice ? "touchend" : "mouseup", mouseAction, true);
		let elementosArrastrables = document.getElementsByClassName("arrastrable");for(let elementoArrastrable of elementosArrastrables){elementoArrastrable.addEventListener(isTouchDevice ? "touchstart" : "mousedown", function(event){let limitarMovimiento = false;if (!event.target.hasOwnProperty("intervalo")) {event.target.intervalo = false;}if (!event.target.hasOwnProperty("intervaloActual")) {event.target.intervaloActual = 0;}if (!event.target.hasOwnProperty("mouseup")) {event.target.mouseup = false;}if (!event.target.hasOwnProperty("limitarMovimiento")) {event.target.limitarMovimiento = false;}var diferencia_x = document_mouse_x-event.target.getBoundingClientRect().x;var diferencia_y = document_mouse_y-event.target.getBoundingClientRect().y;if(event.target.intervaloActual>0){clearInterval(event.target.intervalo)}event.target.intervalo=setInterval(() => {let left = document_mouse_x-diferencia_x;let top = document_mouse_y-diferencia_y;if(event.target.limitarMovimiento){let width = event.target.getBoundingClientRect().width; let height = event.target.getBoundingClientRect().height;let maxleft = window.innerWidth-width; let maxtop = window.innerHeight-height;if(left<0){left=0};if(top<0){top=0}if(left>maxleft){left = maxleft};if(top>maxtop){top=maxtop}}event.target.style.left =  left + "px";event.target.style.top =  top + "px";event.target.intervaloActual+=1;}, 30);if(!event.target.mouseup){event.target.addEventListener(isTouchDevice ? "touchend" : "mouseup", function(){clearInterval(event.target.intervalo);event.target.intervaloActual = 0;event.target.mouseup = true;});}});}

/*
  Con este código, podras arrastrar a todos los elementos que tengan la clase "arrastrable".
*/
